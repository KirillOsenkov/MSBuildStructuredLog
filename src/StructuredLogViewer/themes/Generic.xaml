<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:l="clr-namespace:Microsoft.Build.Logging.StructuredLogger;assembly=StructuredLogger"
                    xmlns:local="clr-namespace:Microsoft.Build.Logging.StructuredLogger"
                    xmlns:core="clr-namespace:Microsoft.Build.Logging.StructuredLogger;assembly=StructuredLogViewer.Core"
                    xmlns:controls="clr-namespace:StructuredLogViewer.Controls"
                    xmlns:s="clr-namespace:StructuredLogViewer">

  <Geometry x:Key="SearchGeometry">F1 M 8.5,7.0 C 7.12,7.00 6.0,5.88 6.0,4.5 C 6.0,3.12 7.12,2.0 8.5,2.0 C 9.88,2.0 11.0,3.12 11.0,4.5 C 11.0,5.88 9.88,7.0 8.5,7.0 Z M 8.5,0.0 C 6.02,0.0 4.0,2.02 4.0,4.500 C 4.0,5.23 4.19,5.9 4.49,6.5 L 0.0,11.0 L 2.0,13.0 L 6.49,8.51 C 7.1,8.81 7.77,9.0 8.5,9.0 C 11.0,9.0 13.0,7.0 13.0,4.5 C 13.0,2.02 11.0,0.0 8.5,0.0 Z</Geometry>
  <BooleanToVisibilityConverter x:Key="booleanToVisibilityConverter" />

  <Geometry x:Key="AddItemGeometry">M 2 4 L 5 4 L 5 1 L 8 1 L 8 4 L 11 4 L 11 7 L 8 7 L 8 10 L 5 10 L 5 7 L 2 7 Z</Geometry>
  <Geometry x:Key="RemoveItemGeometry">M 2 4 L 11 4 L 11 7 L 2 7 Z</Geometry>
  <Geometry x:Key="SquareIconGeometry">F1M0,0L15,0L15,15L0,15Z</Geometry>

  <Geometry x:Key="NuGetGeometry">F1 M11.433,4 L7.568,4 A3.569,3.569 0 0 0 4,7.568L4,11.433 A3.568,3.568 0 0 0 7.568,15L11.433,15 A3.567,3.567 0 0 0 15,11.433L15,7.568 A3.568,3.568 0 0 0 11.433,4M7,8.447 A1.447,1.447 0 1 1 8.447,7A1.447,1.447 0 0 1 7,8.447M11.484,13.8 A2.316,2.316 0 1 1 13.8,11.484A2.316,2.316 0 0 1 11.484,13.8M3.665,2.5 A1.165,1.165 0 1 1 2.5,1.335A1.165,1.165 0 0 1 3.665,2.5</Geometry>

  <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
  </Style>

  <Style x:Key="{x:Type Button}" TargetType="{x:Type Button}">
    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
  </Style>

  <Style x:Key="{x:Type CheckBox}" TargetType="{x:Type CheckBox}">
    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}" />
  </Style>

  <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}">
    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
  </Style>

  <Style x:Key="{x:Type Menu}" TargetType="{x:Type Menu}">
    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}"/>
    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}"/>
  </Style>

  <Style x:Key="{x:Type MenuItem}" TargetType="{x:Type MenuItem}">
    <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.MenuBarBrushKey}}"/>
    <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.MenuTextBrushKey}}"/>
  </Style>

  <SolidColorBrush x:Key="FolderStroke" Color="Goldenrod" />
  <SolidColorBrush x:Key="TargetStroke" Color="MediumPurple" />
  <SolidColorBrush x:Key="TaskStroke" Color="DodgerBlue" />
  <SolidColorBrush x:Key="ParameterStroke" Color="DodgerBlue" />
  <SolidColorBrush x:Key="PropertyStroke" Color="DodgerBlue" />
  <SolidColorBrush x:Key="ItemStroke" Color="MediumAquamarine" />
  <SolidColorBrush x:Key="AddItemStroke" Color="Teal" />
  <SolidColorBrush x:Key="MetadataStroke" Color="Teal" />
  <SolidColorBrush x:Key="MessageStroke" Color="LightGray" />
  <SolidColorBrush x:Key="ImportStroke" Color="Sienna" />
  <SolidColorBrush x:Key="NoImportStroke" Color="Red" />
  <SolidColorBrush x:Key="NoImportFill" Color="BlanchedAlmond" />
  <SolidColorBrush x:Key="ErrorStroke" Color="Red" />
  <SolidColorBrush x:Key="WarningStroke" Color="Gold" />
  <SolidColorBrush x:Key="SearchResultStroke" Color="OrangeRed" />
  <SolidColorBrush x:Key="ContainsSearchResultStroke" Color="LightCoral" />
  <SolidColorBrush x:Key="TargetFrameworkBackground" Color="#E5F0E5" />
  <SolidColorBrush x:Key="TargetFrameworkForeground" Color="#274E13" />

  <LinearGradientBrush x:Key="ClosedFolderBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="FloralWhite" Offset="0" />
    <GradientStop Color="NavajoWhite" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="TargetBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="LavenderBlush" Offset="0" />
    <GradientStop Color="Thistle" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="TaskBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="Cyan" Offset="0" />
    <GradientStop Color="DeepSkyBlue" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="ParameterBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="AliceBlue" Offset="0" />
    <GradientStop Color="LightSkyBlue" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="ItemBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="Honeydew" Offset="0" />
    <GradientStop Color="LightGreen" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="MetadataBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="PaleTurquoise" Offset="0" />
    <GradientStop Color="LightSeaGreen" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="PropertyBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="AliceBlue" Offset="0" />
    <GradientStop Color="LightSkyBlue" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="ImportBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="Bisque" Offset="0" />
    <GradientStop Color="Tan" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="NoImportBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="Bisque" Offset="0" />
    <GradientStop Color="Tomato" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="MessageBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="White" Offset="0" />
    <GradientStop Color="LightGray" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="ErrorBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="MistyRose" Offset="0" />
    <GradientStop Color="LightCoral" Offset="1" />
  </LinearGradientBrush>

  <LinearGradientBrush x:Key="WarningBrush" StartPoint="0,0" EndPoint="1,1">
    <GradientStop Color="LightYellow" Offset="0" />
    <GradientStop Color="Yellow" Offset="1" />
  </LinearGradientBrush>

  <SolidColorBrush x:Key="SearchResultBrush" Color="DarkOrange" />
  <SolidColorBrush x:Key="ContainsSearchResultBrush" Color="Bisque" />

  <DrawingBrush x:Key="SlnIcon">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <DrawingGroup.Children>
          <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
          <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M0,-0.000199999999999534L0,8.5858 0,13.4148 0,13.9998 0.586,13.9998 2,15.4138 3.414,13.9998 4.585,13.9998 6.586,15.9998 7.236,15.9998 10,14.6188 10,13.9998 16,13.9998 16,-0.000199999999999534z" />
          <GeometryDrawing Brush="#FF414141" Geometry="F1M15,1L15,13 10,13 10,12 14,12 14,4 2,4 2,6.586 1,7.586 1,1z" />
          <GeometryDrawing Brush="#FF672079" Geometry="F1M5,11L7,9 7,13z M2,12L2,10 3,11z M7,7L4,10 2,8 1,9 1,13 2,14 4,12 7,15 9,14 9,8z" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M1.9998,12.0004L2.9998,10.9994 1.9998,9.9994z" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M7,13L7,9 5,11z" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M2,4L2,6.586 4,8.586 6.803,5.783 10,7.382 10,12 14,12 14,4z" />
        </DrawingGroup.Children>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>

  <DrawingBrush x:Key="CSProjIcon">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <DrawingGroup.Children>
          <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
          <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M0,15L16,15 16,1 0,1z" />
          <GeometryDrawing Brush="#FF414141" Geometry="F1M10,7L11,7 11,8 10,8z M9,10L10,10 10,9 11,9 11,10 12,10 12,9 13,9 13,8 12,8 12,7 13,7 13,6 12,6 12,5 11,5 11,6 10,6 10,5 9,5 9,6 8,6 8,7 9,7 9,8 8,8 8,9 9,9z" />
          <GeometryDrawing Brush="#FF414141" Geometry="F1M5.8496,11C6.5296,11,7.0786,10.898,7.4996,10.692L7.4996,9.499C7.0786,9.747 6.6156,9.871 6.1076,9.871 5.5766,9.871 5.1546,9.705 4.8396,9.372 4.5256,9.039 4.3696,8.592 4.3696,8.031 4.3696,7.445 4.5356,6.981 4.8686,6.639 5.2016,6.297 5.6386,6.125 6.1796,6.125 6.6736,6.125 7.1146,6.241 7.4996,6.473L7.4996,5.215C7.1146,5.072 6.6386,5 6.0766,5 5.1656,5 4.4256,5.289 3.8556,5.866 3.2856,6.443 2.9996,7.195 2.9996,8.124 2.9996,8.995 3.2536,9.692 3.7616,10.216 4.2676,10.739 4.9646,11 5.8496,11" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M10,8L11,8 11,7 10,7z" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M13,7L12,7 12,8 13,8 13,9 12,9 12,10 11,10 11,9 10,9 10,10 9,10 9,9 8,9 8,8 9,8 9,7 8,7 8,6 9,6 9,5 10,5 10,6 11,6 11,5 12,5 12,6 13,6z M7.5,6.473C7.114,6.241 6.674,6.125 6.18,6.125 5.639,6.125 5.201,6.297 4.868,6.639 4.535,6.981 4.369,7.445 4.369,8.031 4.369,8.592 4.525,9.039 4.84,9.372 5.154,9.705 5.576,9.871 6.107,9.871 6.615,9.871 7.079,9.747 7.5,9.499L7.5,10.692C7.079,10.898 6.529,11 5.85,11 4.965,11 4.268,10.739 3.762,10.216 3.254,9.692 3,8.995 3,8.124 3,7.195 3.285,6.443 3.855,5.866 4.426,5.289 5.166,5 6.076,5 6.639,5 7.114,5.072 7.5,5.215z M2,13L14,13 14,3 2,3z" />
          <GeometryDrawing Brush="#FF1F801F" Geometry="F1M14,13L2,13 2,3 14,3z M1,14L15,14 15,2 1,2z" />
        </DrawingGroup.Children>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>

<DrawingBrush x:Key="CppProjIcon">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <DrawingGroup.Children>
          <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
          <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M0,15L16,15 16,1 0,1z" />
          <GeometryDrawing Brush="#FFA43FB1" Geometry="F1M14,13L2,13 2,3 14,3z M1,14L15,14 15,2 1,2z" />
                    <GeometryDrawing Brush="#FFA43FB1" Geometry="F1M8,6V7H6V9H5V7H3V6H5V4H6V6Z" />
                    <GeometryDrawing Brush="#FFA43FB1" Geometry="F1M13,9v1H11v2H10V10H8V9h2V7h1V9Z" />
        </DrawingGroup.Children>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>

  <DrawingBrush x:Key="FSProjIcon">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
        <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M0,15L16,15 16,1 0,1z" />
        <GeometryDrawing Brush="#FF414141" Geometry="M10 7L11 7 11 8 10 8z M9 10L10 10 10 9 11 9 11 10 12 10 12 9 13 9 13 8 12 8 12 7 13 7 13 6 12 6 12 5 11 5 11 6 10 6 10 5 9 5 9 6 8 6 8 7 9 7 9 8 8 8 8 9 9 9z" />
        <GeometryDrawing Brush="#FF414141" Geometry="m 3.2120982 10.993424 h 1.667074 V 8.6677313 H 7.2784249 V 7.5728855 H 4.8791722 V 6.1033972 H 7.4888409 V 5.0059849 H 3.2120982 Z" />
        <GeometryDrawing Brush="#FF682878" Geometry="F1M14,13L2,13 2,3 14,3z M1,14L15,14 15,2 1,2z" />
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>

  <DrawingBrush x:Key="VBProjIcon">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <GeometryDrawing Brush="#00FFFFFF" Geometry="F1M16,16L0,16 0,0 16,0z" />
        <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M0,15L16,15 16,1 0,1z" />
        <GeometryDrawing Brush="#FF414141" Geometry="M 6.9070592 5.4578905 H 8.0003989 L 6.4028881 10.54319 H 5.2194666 L 3.6415708 5.4578905 H 4.7668747 L 5.7330818 8.997259 c 0.052305 0.1910611 0.083543 0.3603303 0.093715 0.5070777 h 0.01961 c 0.015275 -0.1583708 0.047948 -0.3327243 0.100979 -0.5216069 z m 5.4521678 3.5669751 c 0 0.4663945 -0.190335 0.8354421 -0.57028 1.1078674 -0.380671 0.274606 -0.899371 0.410457 -1.558281 0.410457 H 8.0003989 V 5.4578905 h 2.1125781 c 0.647286 0 1.144919 0.1046132 1.493626 0.3123827 0.348706 0.2077717 0.522333 0.5012653 0.522333 0.8797571 0 0.2738809 -0.106065 0.5136153 -0.318195 0.719207 -0.212129 0.205591 -0.48165 0.3487061 -0.810742 0.4293444 v 0.014511 c 0.411183 0.045039 0.742454 0.1787117 0.988727 0.4017385 0.246998 0.2215742 0.370501 0.4918219 0.370501 0.8100162 z M 9.3073212 7.5072665 h 0.5746387 c 0.2695191 0 0.4823771 -0.057389 0.6378421 -0.1714475 C 10.675267 7.2210365 10.753 7.0626663 10.753 6.8628863 10.753 6.4880269 10.434079 6.3027768 9.7976898 6.3027768 H 9.3073212 Z M 10.98329 9.0110623 c 0 -0.2026866 -0.08354 -0.3617826 -0.252811 -0.4787443 C 10.561937 8.4175353 10.325834 8.3594185 10.023621 8.3594185 H 9.3073212 v 1.3403398 h 0.7083098 c 0.303665 0 0.539041 -0.061026 0.711215 -0.1845219 0.170719 -0.1235 0.256444 -0.2913151 0.256444 -0.5041741 z" />
        <GeometryDrawing Brush="#FF00539C" Geometry="F1M14,13L2,13 2,3 14,3z M1,14L15,14 15,2 1,2z" />
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>

  <DrawingBrush x:Key="GenericProjectIcon">
    <DrawingBrush.Drawing>
      <DrawingGroup>
        <DrawingGroup.Children>
          <GeometryDrawing Brush="#FFF6F6F6" Geometry="F1M13.5469,0L7.9999,0C7.0309,0,5.9999,0.701,5.9999,2L1.9999,2C1.0309,2,-9.99999999997669E-05,2.701,-9.99999999997669E-05,4L-9.99999999997669E-05,12C-9.99999999997669E-05,13.299,1.0309,14,1.9999,14L3.9999,14C3.9999,15.299,5.0309,16,5.9999,16L11.9999,16C13.2989,16,13.9999,14.97,13.9999,14L13.9999,12C15.2989,12,15.9999,10.97,15.9999,10L15.9999,2.062z" />
          <GeometryDrawing Brush="#FF414141" Geometry="F1M12,9L12,10 12,14 6,14 6,6 10,6 10,8 12,8z M10.625,5L5.965,5C5.965,5,5,5,5,6L5,14C5,15,5.965,15,5.965,15L11.965,15C12.965,15,13,14,13,14L13,7.137z" />
          <GeometryDrawing Brush="#FF414141" Geometry="F1M4,12L2,12 2,4 6,4 7.736,4 6.625,3 1.965,3C1.965,3,1,3,1,4L1,12C1,13,1.965,13,1.965,13L4,13z" />
          <GeometryDrawing Brush="#FF414141" Geometry="F1M12.625,1L7.965,1C7.965,1,7,1,7,2L7.547,2 8,2.381 8,2 12,2 12,4 14,4 14,5 14,6 14,6.062 14,10 14,10.995C14.962,10.97,15,10,15,10L15,3.137z" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M6,6L10,6 10,7.96 12,7.996 12,14 6,14z" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M4,6C4,4.701,5.031,4,6,4L2,4 2,12 4,12z" />
          <GeometryDrawing Brush="#FFF0EFF1" Geometry="F1M12,3.96L12,2 8,2 8,2.381 9.927,4 11.547,4 14,6.062 14,3.996z" />
        </DrawingGroup.Children>
      </DrawingGroup>
    </DrawingBrush.Drawing>
  </DrawingBrush>
  
  <Style x:Key="NodeIcon" TargetType="{x:Type Rectangle}">
    <Setter Property="Width" Value="14" />
    <Setter Property="Height" Value="11" />
    <Setter Property="Margin" Value="3,3,6,3" />
    <Setter Property="StrokeThickness" Value="1" />
    <Setter Property="VerticalAlignment" Value="Top" />
    <Setter Property="SnapsToDevicePixels" Value="True" />
  </Style>

  <HierarchicalDataTemplate DataType="{x:Type l:Folder}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" x:Name="folderRoot">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource FolderStroke}" Fill="{DynamicResource ClosedFolderBrush}" />
      <TextBlock x:Name="nameText" Text="{Binding Name}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="folderRoot" Property="Opacity" Value="0.25" />
      </DataTrigger>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="nameText" Property="Foreground" Value="DarkGoldenrod" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Name}" Value="DoubleWrites">
        <Setter TargetName="nameText" Property="Foreground" Value="Red" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ErrorBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Name}" Value="Circular Project References">
        <Setter TargetName="nameText" Property="Foreground" Value="Red" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ErrorBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Name}" Value="DoubleBuild">
        <Setter TargetName="nameText" Property="Foreground" Value="Red" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ErrorBrush}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:TimedNode}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource FolderStroke}" Fill="{DynamicResource ClosedFolderBrush}" />
      <TextBlock x:Name="nameText" Text="{Binding Name}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="nameText" Property="Foreground" Value="DarkGoldenrod" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Project}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" x:Name="projectRoot">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}"
                 Width="16" Height="16" Margin="2,2,5,2" StrokeThickness="0"
                 Fill="{StaticResource GenericProjectIcon}" />
      <TextBlock Text="{Binding Name}" Margin="0,2,0,0" ToolTip="{Binding ToolTip}" ToolTipService.ShowDuration="60000" />
      <TextBlock
          Text="{Binding AdornmentString}"
          Margin="6,0,0,0"
          Padding="2,0,2,0"
          FontSize="10"
          VerticalAlignment="Center"
          Foreground="{DynamicResource TargetFrameworkForeground}"
          Background="{DynamicResource TargetFrameworkBackground}"
          Visibility="{Binding AdornmentString, Converter={x:Static s:StringEmptinessToVisibilityConverter.Instance}}"/>
      <controls:NodeHyperlinkControl
            Margin="0,2,0,0"
            Text="{Binding TargetsDisplayText}"
            Foreground="{DynamicResource TargetStroke}"
            ToolTip="Initial targets"
            Visibility="{Binding TargetsDisplayText, Converter={x:Static s:StringEmptinessToVisibilityConverter.Instance}}" />
      <controls:NodeHyperlinkControl
            Margin="6,2,0,0"
            Text="{Binding EvaluationText}"
            HyperlinkKind="Evaluation"
            Opacity="0.5"
            ToolTip="Navigate to the evaluation for this project"
            Visibility="{Binding EvaluationText, Converter={x:Static s:StringEmptinessToVisibilityConverter.Instance}}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="projectRoot" Property="Opacity" Value="0.25" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".sln">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource SlnIcon}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".csproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource CSProjIcon}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".vbproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource VBProjIcon}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".fsproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource FSProjIcon}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".vcxproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource CppProjIcon}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:ProjectEvaluation}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" x:Name="projectRoot">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}"
                 Width="16" Height="16" Margin="2,2,6,2" StrokeThickness="0"
                 Fill="{StaticResource GenericProjectIcon}" />
<!--      <ProgressBar Width="40"
                   Height="9"
                   Margin="2,1,6,0"
                   Minimum="0"
                   Maximum="100"
                   Value="{Binding RelativeDuration}"
                   VerticalAlignment="Center"
                   ToolTip="{Binding ToolTip}"
                   ToolTipService.ShowDuration="60000" />-->
      <TextBlock Text="{Binding Name}" Margin="0,2,0,0" ToolTip="{Binding ToolTip}" ToolTipService.ShowDuration="60000" />
      <TextBlock
          Text="{Binding AdornmentString}"
          Margin="6,0,0,0"
          Padding="2,0,2,0"
          FontSize="10"
          VerticalAlignment="Center"
          Foreground="{DynamicResource TargetFrameworkForeground}"
          Background="{DynamicResource TargetFrameworkBackground}"
          Visibility="{Binding AdornmentString, Converter={x:Static s:StringEmptinessToVisibilityConverter.Instance}}"/>
      <TextBlock
          Text="{Binding EvaluationText}"
          Margin="6,2,0,0"
          Opacity="0.5"
          ToolTip="Evaluation Id"
          Visibility="{Binding EvaluationText, Converter={x:Static s:StringEmptinessToVisibilityConverter.Instance}}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="projectRoot" Property="Opacity" Value="0.25" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".csproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource CSProjIcon}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".vbproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource VBProjIcon}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".fsproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource FSProjIcon}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectFileExtension}" Value=".vcxproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource CppProjIcon}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Target}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" x:Name="targetRoot">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource TargetStroke}" Fill="{DynamicResource TargetBrush}" />
      <TextBlock x:Name="label" Text="Target " />
      <TextBlock Text="{Binding Name}" />
      <TextBlock x:Name="duration" Text="{Binding DurationText}" Margin="6,0,0,0" ToolTip="{Binding ToolTip}" />
      <controls:NodeHyperlinkControl
            Text="{Binding ParentTargetText}"
            Foreground="{DynamicResource TargetStroke}"
            ToolTip="{Binding ParentTargetTooltip}"
            ToolTipService.InitialShowDelay="500"
            ToolTipService.ShowDuration="300000" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="targetRoot" Property="Opacity" Value="0.25" />
      </DataTrigger>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource TargetStroke}" />
        <Setter TargetName="duration" Property="Foreground" Value="LightGray" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <DataTemplate DataType="{x:Type l:EntryTarget}">
    <StackPanel Orientation="Horizontal" x:Name="entryTargetRoot">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource TargetStroke}" Fill="{DynamicResource TargetBrush}" />
      <TextBlock Text="{Binding Name}" />
      <TextBlock x:Name="duration" Text="{Binding DurationText}" Margin="6,0,0,0" ToolTip="{Binding ToolTip}" />
      <controls:NodeHyperlinkControl
          Text=" → Show"
          Foreground="LightBlue" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="entryTargetRoot" Property="Opacity" Value="0.25" />
      </DataTrigger>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="duration" Property="Foreground" Value="LightGray" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </DataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Task}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource TaskStroke}" Fill="{DynamicResource TaskBrush}" />
      <TextBlock x:Name="label" Text="Task " />
      <TextBlock Text="{Binding Name}" />
      <TextBlock x:Name="duration" Text="{Binding DurationText}" Margin="6,0,0,0" ToolTip="{Binding ToolTip}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource TaskStroke}" />
        <Setter TargetName="duration" Property="Foreground" Value="LightGray" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:AddItem}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Path x:Name="icon"
          Data="{DynamicResource AddItemGeometry}"
          VerticalAlignment="Center"
          Width="14"
          Height="11"
          Margin="3,3,6,3"
          Stroke="{DynamicResource ItemStroke}"
          Fill="{DynamicResource ItemBrush}" />
      <TextBlock x:Name="label" Text="Add Item " />
      <TextBlock Text="{Binding Name}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource AddItemStroke}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:TaskParameterItem}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Path x:Name="icon"
          Data="{DynamicResource AddItemGeometry}"
          VerticalAlignment="Center"
          Width="14"
          Height="11"
          Margin="3,3,6,3"
          Stroke="{DynamicResource ItemStroke}"
          Fill="{DynamicResource ItemBrush}" />
      <TextBlock x:Name="label" Text="Add Item " />
      <TextBlock Text="{Binding Name}" />
      <TextBlock x:Name="parameterName" Text="{Binding ParameterName}" Foreground="Gray" Margin="16,0,0,0" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource AddItemStroke}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:RemoveItem}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Path x:Name="icon"
          Data="{DynamicResource RemoveItemGeometry}"
          VerticalAlignment="Center"
          Width="14"
          Height="11"
          Margin="3,3,6,3"
          Stroke="{DynamicResource ItemStroke}"
          Fill="{DynamicResource ItemBrush}" />
      <TextBlock x:Name="label" Text="Remove Item " />
      <TextBlock Text="{Binding Name}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource AddItemStroke}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Item}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource ItemStroke}" Fill="{DynamicResource ItemBrush}" />
      <TextBlock Text="{Binding ShortenedName}" />
    </StackPanel>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:FileCopy}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Path x:Name="icon"
            Data="M5,5 L5,11 L11,11 L11,5 Z M 0,8 L16,8"
            Stroke="Thistle"
            VerticalAlignment="Center"
            StrokeThickness="1"
            Width="16"
            Height="16"
            Margin="1,2,6,2"
            Fill="Thistle" />
      <TextBlock Text="{Binding ShortenedName}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding Kind}" Value="Source">
        <Setter TargetName="icon" Property="Data" Value="M0,5 L0,11 L6,11 L6,5 Z M 6,8 L16,8" />
        <Setter TargetName="icon" Property="Stroke" Value="LightSkyBlue" />
        <Setter TargetName="icon" Property="Fill" Value="LightSkyBlue" />
      </DataTrigger>
      <DataTrigger Binding="{Binding Kind}" Value="Destination">
        <Setter TargetName="icon" Property="Data" Value="M10,5 L10,11 L16,11 L16,5 Z M 0,8 L10,8" />
        <Setter TargetName="icon" Property="Stroke" Value="DarkSalmon" />
        <Setter TargetName="icon" Property="Fill" Value="DarkSalmon" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Package}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Path Data="{DynamicResource NuGetGeometry}"
            Stroke="{DynamicResource NuGet}"
            VerticalAlignment="Center"
            StrokeThickness="1"
            Width="16"
            Height="16"
            Margin="1,2,0,2"
            Fill="{DynamicResource NuGet}" />
      <TextBlock Text="{Binding ShortenedName}" Margin="6,0,0,0" VerticalAlignment="Center" />
      <TextBlock Text="{Binding Version}" Margin="6,0,0,0" Foreground="{DynamicResource PropertyStroke}" VerticalAlignment="Center" />
      <TextBlock Text="{Binding VersionSpec}" Margin="6,0,0,0" Foreground="{DynamicResource ImportStroke}" VerticalAlignment="Center" />
    </StackPanel>
  </HierarchicalDataTemplate>

  <DataTemplate DataType="{x:Type l:Metadata}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource MetadataStroke}" Fill="{DynamicResource MetadataBrush}" />
      <TextBlock x:Name="label" Text="{Binding NameAndEquals, Mode=OneTime}" />
      <TextBlock Text="{Binding ShortenedValue}" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="LightSeaGreen" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </DataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:EvaluationProfileEntry}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <ProgressBar Width="40"
                   Height="9"
                   Margin="2,1,6,0"
                   Minimum="0"
                   Maximum="100"
                   Value="{Binding Value}"
                   VerticalAlignment="Center" />
      <TextBlock x:Name="label" 
                 VerticalAlignment="Center"
                 Text="{Binding Title, Mode=OneTime}" />
      <TextBlock x:Name="hits" 
                 VerticalAlignment="Center"
                 Margin="6,0,0,0"
                 Text="{Binding DurationText, Mode=OneTime}" />
      <TextBlock x:Name="code"
                 Text="{Binding ShortenedElementDescription}" 
                 VerticalAlignment="Center"
                 Margin="12,0,0,0"
                 FontFamily="Consolas" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="Black" />
        <Setter TargetName="hits" Property="Foreground" Value="Blue" />
        <Setter TargetName="code" Property="Foreground" Value="Gray" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <DataTemplate DataType="{x:Type l:Property}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource PropertyStroke}" Fill="{DynamicResource PropertyBrush}" />
      <TextBlock x:Name="label" Text="{Binding NameAndEquals, Mode=OneTime}" />
      <TextBlock Text="{Binding ShortenedValue}" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource PropertyStroke}" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </DataTemplate>

  <DataTemplate DataType="{x:Type l:TaskParameterProperty}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource PropertyStroke}" Fill="{DynamicResource PropertyBrush}" />
      <TextBlock x:Name="label" Text="{Binding NameAndEquals, Mode=OneTime}" />
      <TextBlock Text="{Binding ShortenedValue}" />
      <TextBlock x:Name="parameterName" Text="{Binding ParameterName}" Foreground="Gray" Margin="16,0,0,0" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource PropertyStroke}" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </DataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Parameter}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource ParameterStroke}" Fill="{DynamicResource ParameterBrush}" />
      <TextBlock x:Name="name" Text="{Binding Name}" />
      <TextBlock x:Name="parameterName" Text="{Binding ParameterName}" Foreground="Gray" Margin="16,0,0,0" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="name" Property="Foreground" Value="{DynamicResource ParameterStroke}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Message}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource MessageStroke}" Fill="{DynamicResource MessageBrush}" />
      <TextBlock x:Name="messageText" Text="{Binding ShortenedText}" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="messageText" Property="Opacity" Value="0.25" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:TimedMessage}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource MessageStroke}" Fill="{DynamicResource MessageBrush}" />
      <TextBlock x:Name="timeStamp" Text="{Binding TimestampText}" Opacity="0.3" Margin="0,0,8,0" />
      <TextBlock x:Name="messageText" Text="{Binding ShortenedText}" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="messageText" Property="Opacity" Value="0.25" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Import}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" x:Name="importPanel">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource ImportStroke}" Fill="{DynamicResource ImportBrush}" />
      <TextBlock x:Name="label" Text="Import" Margin="0,0,6,0" />
      <TextBlock x:Name="text" Text="{Binding Text}" />
      <TextBlock x:Name="location" Text="{Binding Location}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="importPanel" Property="Opacity" Value="0.25" />
      </DataTrigger>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource ImportStroke}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:NoImport}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" x:Name="importPanel">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource NoImportStroke}" Fill="{DynamicResource NoImportBrush}" />
      <TextBlock x:Name="label" Text="NoImport" Margin="0,0,6,0" />
      <TextBlock x:Name="text" Text="{Binding Text}" />
      <TextBlock x:Name="location" Text="{Binding Location}" Margin="0,0,6,0" />
      <TextBlock x:Name="reason" Text="{Binding Reason}" />
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="importPanel" Property="Opacity" Value="0.3" />
      </DataTrigger>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="label" Property="Foreground" Value="{DynamicResource NoImportStroke}" />
        <Setter TargetName="reason" Property="Background" Value="{DynamicResource NoImportFill}" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <DataTemplate DataType="{x:Type core:ButtonNode}">
    <StackPanel Orientation="Horizontal">
      <Button x:Name="button"
              Content="{Binding Text}"
              Command="{Binding Command}"
              IsEnabled="{Binding IsEnabled}"
              Margin="3"
              Padding="4,2,4,2" />
    </StackPanel>
  </DataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Error}"  ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource ErrorStroke}" Fill="{DynamicResource ErrorBrush}" />
      <TextBlock x:Name="text" Text="{Binding}" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="False">
        <Setter TargetName="text" Property="Foreground" Value="{DynamicResource ErrorStroke}" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:CriticalBuildMessage}"  ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource WarningStroke}" Fill="{DynamicResource WarningBrush}" />
      <TextBlock x:Name="text" Text="{Binding}" />
    </StackPanel>
  </HierarchicalDataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:Warning}" ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource WarningStroke}" Fill="{DynamicResource WarningBrush}" />
      <TextBlock Text="{Binding}" />
    </StackPanel>
  </HierarchicalDataTemplate>

  <DataTemplate DataType="{x:Type l:Note}">
    <StackPanel Orientation="Horizontal">
      <Border BorderBrush="LightGray" BorderThickness="1" Margin="3" Background="{DynamicResource Theme_InfoBarBackground}" Padding="6">
        <TextBlock Text="{Binding Text}"
                   Foreground="Gray"
                   TextWrapping="Wrap"/>
      </Border>
    </StackPanel>
  </DataTemplate>

  <DataTemplate DataType="{x:Type core:CustomContentNode}">
    <StackPanel Orientation="Horizontal">
      <ContentControl Content="{Binding Content}" />
    </StackPanel>
  </DataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:SourceFile}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal">
      <Rectangle x:Name="icon" Style="{StaticResource NodeIcon}" Stroke="{DynamicResource MessageStroke}" Fill="{DynamicResource MessageBrush}" />
      <TextBlock x:Name="messageText" Text="{Binding Name}" />
      <TextBlock Text="{Binding SourceFilePath}" Opacity="0.4" Margin="8,0,0,0" />
    </StackPanel>
  </HierarchicalDataTemplate>

  <DataTemplate DataType="{x:Type l:SourceFileLine}">
    <StackPanel Orientation="Horizontal">
      <TextBlock x:Name="lineNumber" Text="{Binding LineNumber}" FontFamily="Consolas" Foreground="Teal" Margin="0,0,12,0" />
      <TextBlock x:Name="messageText" Text="{Binding LineText}" FontFamily="Consolas" />
    </StackPanel>
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsSelected}" Value="True">
        <Setter TargetName="lineNumber" Property="Foreground" Value="White" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </DataTemplate>

  <DataTemplate DataType="{x:Type l:HighlightedText}">
    <TextBlock x:Name="text" Foreground="Black" Background="Yellow" Text="{Binding Text}" />
    <DataTemplate.Triggers>
      <DataTrigger Binding="{Binding Style}" Value="time">
        <Setter TargetName="text" Property="Background" Value="Lavender" />
      </DataTrigger>
    </DataTemplate.Triggers>
  </DataTemplate>

  <HierarchicalDataTemplate DataType="{x:Type l:ProxyNode}"
                            ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" x:Name="stackPanel">
      <Path x:Name="icon"
          Data="F1M0,0L14,0L14,11L0,11Z"
          Width="14"
          Height="11"
          Margin="3,3,6,3"
          VerticalAlignment="Center"
          Stroke="{DynamicResource ParameterStroke}"
          Fill="{DynamicResource ParameterBrush}" />
      <ItemsControl x:Name="text" ItemsSource="{Binding Highlights}" Focusable="False" VerticalAlignment="Center">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <StackPanel Orientation="Horizontal" IsItemsHost="True" />
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
      </ItemsControl>
    </StackPanel>
    <HierarchicalDataTemplate.Triggers>
      <DataTrigger Binding="{Binding IsLowRelevance}" Value="True">
        <Setter TargetName="stackPanel" Property="Opacity" Value="0.25" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Folder">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource FolderStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ClosedFolderBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Target">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource TargetStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource TargetBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Task">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource TaskStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource TaskBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="AddItem">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource ItemStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ItemBrush}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource AddItemGeometry}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="RemoveItem">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource ItemStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ItemBrush}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource RemoveItemGeometry}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Item">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource ItemStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ItemBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Metadata">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource MetadataStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource MetadataBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Property">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource PropertyStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource PropertyBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Parameter">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource ParameterStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ParameterBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="CriticalBuildMessage">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource WarningStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource WarningBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Message">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource MessageStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource MessageBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Error">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource ErrorStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ErrorBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Warning">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource WarningStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource WarningBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Import">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource ImportStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource ImportBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="NoImport">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource NoImportStroke}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource NoImportBrush}" />
      </DataTrigger>
      <DataTrigger Binding="{Binding OriginalType}" Value="Package">
        <Setter TargetName="icon" Property="Stroke" Value="{DynamicResource NuGet}" />
        <Setter TargetName="icon" Property="Fill" Value="{DynamicResource NuGet}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource NuGetGeometry}" />
        <Setter TargetName="icon" Property="Width" Value="16" />
        <Setter TargetName="icon" Property="Height" Value="16" />
        <Setter TargetName="icon" Property="Margin" Value="1,2,6,2" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectExtension}" Value=".sln">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource SlnIcon}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource SquareIconGeometry}" />
        <Setter TargetName="icon" Property="Height" Value="16" />
        <Setter TargetName="icon" Property="Width" Value="16" />
        <Setter TargetName="icon" Property="StrokeThickness" Value="0" />
        <Setter TargetName="icon" Property="Margin" Value="2,2,6,2" />
        <Setter TargetName="text" Property="Margin" Value="0,2,0,0" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectExtension}" Value=".csproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource CSProjIcon}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource SquareIconGeometry}" />
        <Setter TargetName="icon" Property="Height" Value="16" />
        <Setter TargetName="icon" Property="Width" Value="16" />
        <Setter TargetName="icon" Property="StrokeThickness" Value="0" />
        <Setter TargetName="icon" Property="Margin" Value="2,2,6,2" />
        <Setter TargetName="text" Property="Margin" Value="0,2,0,0" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectExtension}" Value=".vbproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource VBProjIcon}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource SquareIconGeometry}" />
        <Setter TargetName="icon" Property="Height" Value="16" />
        <Setter TargetName="icon" Property="Width" Value="16" />
        <Setter TargetName="icon" Property="StrokeThickness" Value="0" />
        <Setter TargetName="icon" Property="Margin" Value="2,2,6,2" />
        <Setter TargetName="text" Property="Margin" Value="0,2,0,0" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectExtension}" Value=".fsproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource FSProjIcon}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource SquareIconGeometry}" />
        <Setter TargetName="icon" Property="Height" Value="16" />
        <Setter TargetName="icon" Property="Width" Value="16" />
        <Setter TargetName="icon" Property="StrokeThickness" Value="0" />
        <Setter TargetName="icon" Property="Margin" Value="2,2,6,2" />
        <Setter TargetName="text" Property="Margin" Value="0,2,0,0" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectExtension}" Value=".vcxproj">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource CppProjIcon}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource SquareIconGeometry}" />
        <Setter TargetName="icon" Property="Height" Value="16" />
        <Setter TargetName="icon" Property="Width" Value="16" />
        <Setter TargetName="icon" Property="StrokeThickness" Value="0" />
        <Setter TargetName="icon" Property="Margin" Value="2,2,6,2" />
        <Setter TargetName="text" Property="Margin" Value="0,2,0,0" />
      </DataTrigger>
      <DataTrigger Binding="{Binding ProjectExtension}" Value="other">
        <Setter TargetName="icon" Property="Fill" Value="{StaticResource GenericProjectIcon}" />
        <Setter TargetName="icon" Property="Data" Value="{DynamicResource SquareIconGeometry}" />
        <Setter TargetName="icon" Property="Height" Value="16" />
        <Setter TargetName="icon" Property="Width" Value="16" />
        <Setter TargetName="icon" Property="StrokeThickness" Value="0" />
        <Setter TargetName="icon" Property="Margin" Value="2,2,6,2" />
        <Setter TargetName="text" Property="Margin" Value="0,2,0,0" />
      </DataTrigger>
    </HierarchicalDataTemplate.Triggers>
  </HierarchicalDataTemplate>

  <DataTemplate DataType="{x:Type core:WelcomeScreen}">
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
    <Grid x:Name="welcomeScreen">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition />
      </Grid.RowDefinitions>
      <TextBlock Text="{Binding Version}" 
                 Foreground="Gray" 
                 FontSize="16" 
                 Margin="20"
                 HorizontalAlignment="Right"
                 VerticalAlignment="Top"/>
      <Grid
          Grid.Row="1"
          HorizontalAlignment="Center"
          VerticalAlignment="Center">
        <Rectangle Fill="Black">
          <Rectangle.Effect>
            <DropShadowEffect BlurRadius="40" 
                            Color="LightGray" 
                            Direction="-45" 
                            Opacity="1.0" 
                            RenderingBias="Performance" 
                            ShadowDepth="40" />
          </Rectangle.Effect>
        </Rectangle>
        <Grid Background="{DynamicResource Theme_WhiteBackground}">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>
          <TextBlock x:Name="customMessage" 
                     Grid.Row="0"
                     FontSize="16"
                     Foreground="Red"
                     Margin="10"
                     TextWrapping="Wrap"
                     Visibility="{Binding Message, Converter={x:Static s:StringEmptinessToVisibilityConverter.Instance}}"
                     Text="{Binding Message}"/>
          <Grid x:Name="openLogOrProjectCompartment" 
                Grid.Row="1">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Margin="20"
                  MinWidth="200"
                  MinHeight="200"
                  Command="{Binding OpenProjectCommand}">
              <StackPanel>
                <Rectangle Width="100" Height="100" Margin="20" Fill="{StaticResource SlnIcon}" />
                <TextBlock Text="Open Project/Solution" 
                         FontSize="16" 
                         Margin="10"
                         HorizontalAlignment="Center"/>
              </StackPanel>
            </Button>
            <Button Margin="0,20,20,20"
                  MinWidth="200"
                  MinHeight="200"
                  Command="{Binding OpenLogFileCommand}"
                  Grid.Column="1">
              <StackPanel>
                <Canvas Width="100" Height="100" Margin="20">
                  <Canvas.Effect>
                    <DropShadowEffect BlurRadius="15" Color="Gray" Opacity="0.5" />
                  </Canvas.Effect>
                  <Canvas.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1" />
                  </Canvas.RenderTransform>
                  <Rectangle Width="35" Height="35" Canvas.Left="0" Canvas.Top="0" Stroke="{DynamicResource TargetStroke}" Fill="{DynamicResource TargetBrush}" />
                  <Rectangle Width="35" Height="35" Canvas.Left="25" Canvas.Top="35" Stroke="{DynamicResource ParameterStroke}" Fill="{DynamicResource ParameterBrush}" />
                  <Rectangle Width="35" Height="35" Canvas.Left="25" Canvas.Top="70" Stroke="{DynamicResource TaskStroke}" Fill="{DynamicResource TaskBrush}" />
                  <Rectangle Width="35" Height="35" Canvas.Left="60" Canvas.Top="35" Stroke="{DynamicResource ItemStroke}" Fill="{DynamicResource ItemBrush}" />
                  <Rectangle Width="35" Height="35" Canvas.Left="60" Canvas.Top="70" Stroke="{DynamicResource FolderStroke}" Fill="{DynamicResource ClosedFolderBrush}" />
                  <Line X1="16" Y1="35" X2="16" Y2="87" Stroke="Black" />
                  <Line X1="16" Y1="87" X2="25" Y2="87" Stroke="Black" />
                  <Line X1="16" Y1="53" X2="25" Y2="53" Stroke="Black" />
                </Canvas>
                <TextBlock Text="Open Log File"
                         FontSize="16"
                         Margin="10"
                         HorizontalAlignment="Center" />
              </StackPanel>
            </Button>
          </Grid>
          <Grid x:Name="recentProjects"
            Grid.Row="2"
            Visibility="{Binding ShowRecentProjects, Converter={StaticResource booleanToVisibilityConverter}}">
            <GroupBox Header="Recent projects and solutions"
                  Margin="10">
              <ListBox ItemsSource="{Binding RecentProjects}"
                   Margin="10"
                   SelectedItem="{Binding SelectedProject, Mode=OneWayToSource}"
                   Background="Transparent"
                   BorderBrush="Transparent"
                   BorderThickness="0"/>
            </GroupBox>
          </Grid>
          <Grid x:Name="recentLogs"
            Grid.Row="3"
            Visibility="{Binding ShowRecentLogs, Converter={StaticResource booleanToVisibilityConverter}}">
            <GroupBox Header="Recent logs"
                  Margin="10">
              <ListBox ItemsSource="{Binding RecentLogs}" 
                   Margin="10"
                   SelectedItem="{Binding SelectedLog, Mode=OneWayToSource}"
                   Background="Transparent"
                   BorderBrush="Transparent"
                   BorderThickness="0"/>
            </GroupBox>
          </Grid>
          <StackPanel Grid.Row="4">
            <CheckBox Content="Enable tree virtualization (faster, but may cause hangs)"
                      Margin="10,0,10,10"
                      IsChecked="{Binding EnableVirtualization}"
                      ToolTip="Disable virtualization if you're experiencing hangs or deadlocks when scrolling the tree"/>
            <!--<CheckBox Content="Display all targets directly under the project (flattened in temporal order)"
                      Margin="10,0,10,10"
                      IsChecked="{Binding ParentAllTargetsUnderProject}" 
                      ToolTip="Linearize/flatten all targets under the project instead of parenting targets under their dependency target"/>-->
            <CheckBox Content="Mark search results with a dot in the main tree"
                      Margin="10,0,10,10"
                      IsChecked="{Binding MarkResultsInTree}" 
                      ToolTip="Mark tree nodes that appear in search results with a dot"/>
            <CheckBox Content="Show project configuration and platform"
                      Margin="10,0,10,10"
                      IsChecked="{Binding ShowConfigurationAndPlatform}" 
                      ToolTip="Show project Configuration and Platform property values if available"/>
            <CheckBox Content="Dark Theme"
                      Margin="10,0,10,10"
                      IsChecked="{Binding UseDarkTheme}"
                      ToolTip="Use dark theme"/>
          </StackPanel>
        </Grid>
      </Grid>
    </Grid>
    </ScrollViewer>
  </DataTemplate>

  <DataTemplate DataType="{x:Type local:BuildParametersScreen}">
    <Grid x:Name="buildParameters" 
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Margin="40">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <StackPanel Grid.Row="0"
                  Margin="40,40,40,10">
        <TextBlock Text="Specify custom MSBuild command line arguments:" 
                   Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                   Margin="0,0,0,7"/>
        <WrapPanel Orientation="Horizontal">
          <ComboBox ItemsSource="{Binding MSBuildLocations}" 
                    IsSynchronizedWithCurrentItem="True"
                    ToolTip="Select MSBuild toolset to use for building"
                    Height="22"
                    IsEditable="False" />
          <Button Command="{Binding BrowseForMSBuildCommand}"
                  Width="22"
                  Height="22"
                  ToolTip="Browse for custom MSBuild file location..."
                  Margin="0,0,8,0">
            <StackPanel Orientation="Horizontal">
              <Rectangle Width="2" Height="2" Margin="2" Fill="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
              <Rectangle Width="2" Height="2" Margin="0,2,2,2" Fill="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
              <Rectangle Width="2" Height="2" Margin="0,2,2,2" Fill="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
            </StackPanel>
          </Button>
          <TextBlock Text="{Binding PrefixArguments}" 
                     Margin="0,3,0,0"
                     Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                     TextWrapping="Wrap" />
          <TextBox x:Name="argumentsText"
                   Margin="4,0,4,0"
                   Padding="0,1,0,0"
                   Text="{Binding MSBuildArguments, UpdateSourceTrigger=PropertyChanged}" 
                   TextWrapping="Wrap"
                   MinWidth="300">
            <TextBox.InputBindings>
              <KeyBinding Key="Enter" 
                          Command="{Binding BuildCommand}" />
            </TextBox.InputBindings>
          </TextBox>

          <TextBlock Text="{Binding PostfixArguments}"
                     Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                     TextWrapping="Wrap"/>
        </WrapPanel>
        <Button Content="Copy command line to clipboard" 
                Command="{Binding CopyCommand}"
                Padding="4,0,4,0"
                Height="23"
                HorizontalAlignment="Right"
                Margin="4" />
      </StackPanel>
      <StackPanel Grid.Row="1"
                  HorizontalAlignment="Center"
                  Margin="40,10,40,40"
                  Orientation="Horizontal">
        <Button Content="Build" Command="{Binding BuildCommand}" Width="75" Height="23" Margin="7" />
        <Button Content="Cancel" Command="{Binding CancelCommand}" Width="75" Height="23" Margin="0,7,7,7" />
      </StackPanel>
    </Grid>
  </DataTemplate>

  <DataTemplate DataType="{x:Type core:BuildProgress}">
    <Grid x:Name="buildProgress" 
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Margin="40">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>
      <TextBlock x:Name="text"
                 Text="{Binding ProgressText}"
                 HorizontalAlignment="Center"
                 Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                 TextWrapping="Wrap" />
      <ProgressBar Grid.Row="1" 
                   IsIndeterminate="{Binding IsIndeterminate}" 
                   Value="{Binding Value}"
                   Minimum="0.0"
                   Maximum="1.0"
                   HorizontalAlignment="Center"
                   Width="200" 
                   Height="23" 
                   Margin="10" />
      <TextBlock x:Name="buffer"
                 Grid.Row="2"
                 Text="{Binding BufferText}"
                 HorizontalAlignment="Center"
                 Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                 TextWrapping="Wrap" />
      <StackPanel Grid.Row="2"
                  Margin="40"
                  Visibility="{Binding ShowCommandLine, Converter={StaticResource booleanToVisibilityConverter}}">
        <TextBlock Text="Using command line (press Ctrl+C to copy):"
                   Margin="0,0,0,7"
                   Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <TextBox Text="{Binding MSBuildCommandLine}" 
                 IsReadOnly="True" 
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 MinHeight="23"
                 IsReadOnlyCaretVisible="True" />
      </StackPanel>
    </Grid>
  </DataTemplate>

  <!--=================================================================
        TreeViewItem
    ==================================================================-->

  <SolidColorBrush x:Key="&#465;" Color="#FF818181"/>
  <SolidColorBrush x:Key="&#466;" Color="#FFFFFFFF"/>
  <SolidColorBrush x:Key="&#467;" Color="#FF27C7F7"/>
  <SolidColorBrush x:Key="&#468;" Color="#FFCCEEFB"/>
  <SolidColorBrush x:Key="&#469;" Color="#FF262626"/>
  <SolidColorBrush x:Key="&#470;" Color="#FF595959"/>
  <SolidColorBrush x:Key="&#471;" Color="#FF1CC4F7"/>
  <SolidColorBrush x:Key="&#472;" Color="#FF82DFFB"/>

  <PathGeometry x:Key="&#473;">
    <PathGeometry.Figures>
      <PathFigureCollection>
        <PathFigure IsFilled="True" StartPoint="0 0" IsClosed="True">
          <PathFigure.Segments>
            <PathSegmentCollection>
              <LineSegment Point="0 6"/>
              <LineSegment Point="6 0"/>
            </PathSegmentCollection>
          </PathFigure.Segments>
        </PathFigure>
      </PathFigureCollection>
    </PathGeometry.Figures>
  </PathGeometry>
  <Style x:Key="&#474;" TargetType="{x:Type ToggleButton}">
    <Setter Property="Focusable" Value="False"/>
    <Setter Property="Width" Value="16"/>
    <Setter Property="Height" Value="16"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ToggleButton}">
          <Border Width="16"
                  Height="16"
                  Background="Transparent"
                  Padding="5,5,5,5">
            <Path x:Name="ExpandPath"
                  Fill="{StaticResource &#466;}"
                  Stroke="{StaticResource &#465;}"
                  Data="{StaticResource &#473;}">
              <Path.RenderTransform>
                <RotateTransform Angle="135" CenterX="3" CenterY="3"/>
              </Path.RenderTransform>
            </Path>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="ExpandPath" Property="RenderTransform">
                <Setter.Value>
                  <RotateTransform Angle="180" CenterX="3" CenterY="3"/>
                </Setter.Value>
              </Setter>
              <Setter TargetName="ExpandPath" Property="Fill" Value="{DynamicResource &#470;}"/>
              <Setter TargetName="ExpandPath" Property="Stroke" Value="{StaticResource &#469;}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="ExpandPath" Property="Stroke" Value="{StaticResource &#467;}"/>
              <Setter TargetName="ExpandPath" Property="Fill" Value="{StaticResource &#468;}"/>
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsMouseOver" Value="True"/>
                <Condition Property="IsChecked" Value="True"/>
              </MultiTrigger.Conditions>
              <Setter TargetName="ExpandPath" Property="Stroke" Value="{StaticResource &#471;}"/>
              <Setter TargetName="ExpandPath" Property="Fill" Value="{StaticResource &#472;}"/>
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="&#475;">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <Rectangle />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type TreeViewItem}"
         TargetType="{x:Type TreeViewItem}">
    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
    <Setter Property="Visibility" Value="{Binding IsVisible, Mode=TwoWay, Converter={StaticResource booleanToVisibilityConverter}}" />
    <Setter Property="Background"
            Value="Transparent"/>
    <Setter Property="HorizontalContentAlignment"
            Value="{Binding Path=HorizontalContentAlignment,RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
    <Setter Property="VerticalContentAlignment"
            Value="{Binding Path=VerticalContentAlignment,RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
    <Setter Property="Padding"
            Value="1,0,0,0"/>
    <Setter Property="Foreground"
            Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
    <Setter Property="FocusVisualStyle"
            Value="{StaticResource &#475;}"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TreeViewItem}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition MinWidth="19" Width="Auto"/>
              <ColumnDefinition Width="Auto"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition/>
            </Grid.RowDefinitions>
            <ToggleButton x:Name="Expander"
                          Style="{StaticResource &#474;}"
                          IsChecked="{Binding Path=IsExpanded,RelativeSource={RelativeSource TemplatedParent}}"
                          ClickMode="Press"/>
            <Border Name="Bd"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    Padding="{TemplateBinding Padding}"
                    SnapsToDevicePixels="true">
              <ContentPresenter x:Name="PART_Header"
                                ContentSource="Header"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
            </Border>
            <ItemsPresenter x:Name="ItemsHost" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"/>
            <Ellipse x:Name="SearchResult"
                     Grid.Column="1"
                     Width="6" Height="6" Margin="1,1,0,0" StrokeThickness="1"
                     HorizontalAlignment="Left" VerticalAlignment="Top"
                     Visibility="Collapsed" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsExpanded" Value="false">
              <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="HasItems" Value="false">
              <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="true">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
              <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>
            <DataTrigger Binding="{Binding ContainsSearchResult}" Value="True">
              <Setter TargetName="SearchResult" Property="Visibility" Value="Visible" />
              <Setter TargetName="SearchResult" Property="Fill" Value="{DynamicResource ContainsSearchResultBrush}" />
              <Setter TargetName="SearchResult" Property="Stroke" Value="{DynamicResource ContainsSearchResultStroke}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding IsSearchResult}" Value="True">
              <Setter TargetName="SearchResult" Property="Visibility" Value="Visible" />
              <Setter TargetName="SearchResult" Property="Fill" Value="{DynamicResource SearchResultBrush}" />
              <Setter TargetName="SearchResult" Property="Stroke" Value="{DynamicResource SearchResultStroke}" />
            </DataTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsSelected" Value="true"/>
                <Condition Property="IsSelectionActive" Value="false"/>
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}"/>
              <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}}"/>
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsSelected" Value="True"/>
                <Condition Property="IsSelectionActive" Value="False"/>
                <Condition Property="controls:ContextMenuUtilities.IsContextMenuOpen" Value="True" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
              <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="false">
              <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Style.Triggers>
      <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="true">
        <Setter Property="ItemsPanel">
          <Setter.Value>
            <ItemsPanelTemplate>
              <VirtualizingStackPanel/>
            </ItemsPanelTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
  </Style>

  <ControlTemplate x:Key="closeButtonTemplate">
    <Border x:Name="border"
            Background="Transparent"
            Visibility="{TemplateBinding Visibility}">
      <Path Width="10"
            Height="8"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Fill="{Binding Path=(TextElement.Foreground), RelativeSource={RelativeSource Self}}"
            Stretch="Uniform"
            Data="F1 M 0,0L 2,0L 5,3L 8,0L 10,0L 6,4L 10,8L 8,8L 5,5L 2,8L 0,8L 4,4L 0,0 Z" />
    </Border>
    <ControlTemplate.Triggers>
      <Trigger SourceName="border" Property="IsMouseOver" Value="true">
        <Setter TargetName="border" Property="Background" Value="LightBlue" />
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>

  <DataTemplate x:Key="SourceFileTabHeaderTemplate">
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <Button x:Uid="HideButton"
              x:Name="HideButton"
              Width="16"
              Height="16"
              Template="{StaticResource closeButtonTemplate}"
              Grid.Column="1"
              Command="{Binding Close}"
              VerticalAlignment="Center"
              Margin="4,2,-2,0"
              ToolTip="Close">
      </Button>
      <TextBlock MinWidth="50" Text="{Binding Header}" VerticalAlignment="Center" />
    </Grid>
  </DataTemplate>

</ResourceDictionary>
