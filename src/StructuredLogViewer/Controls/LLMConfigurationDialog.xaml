<Window x:Class="StructuredLogViewer.Controls.LLMConfigurationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configure LLM Chat"
        Width="550"
        Height="420"
        Background="{DynamicResource Theme_Background}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Endpoint -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Endpoint URL" 
                       FontWeight="Bold" 
                       Margin="0,0,0,4"
                       Foreground="{DynamicResource Theme_Foreground}"/>
            <TextBox x:Name="endpointTextBox"
                     Padding="4"
                     ToolTip="E.g., https://your-resource.openai.azure.com/"
                     Background="{DynamicResource Theme_Background}"
                     Foreground="{DynamicResource Theme_Foreground}"/>
        </StackPanel>

        <!-- Model Name -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Model/Deployment Name" 
                       FontWeight="Bold" 
                       Margin="0,0,0,4"
                       Foreground="{DynamicResource Theme_Foreground}"/>
            <TextBox x:Name="modelTextBox"
                     Padding="4"
                     ToolTip="E.g., gpt-4, claude-sonnet-4-5-2"
                     Background="{DynamicResource Theme_Background}"
                     Foreground="{DynamicResource Theme_Foreground}"/>
        </StackPanel>

        <!-- API Key -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="API Key" 
                       FontWeight="Bold" 
                       Margin="0,0,0,4"
                       Foreground="{DynamicResource Theme_Foreground}"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <PasswordBox x:Name="apiKeyPasswordBox"
                             Grid.Column="0"
                             Padding="4"
                             PasswordChar="●"
                             Background="{DynamicResource Theme_Background}"
                             Foreground="{DynamicResource Theme_Foreground}"
                             ToolTip="Your API key (kept secure)"/>
                
                <TextBox x:Name="apiKeyTextBox"
                         Grid.Column="0"
                         Padding="4"
                         Visibility="Collapsed"
                         Background="{DynamicResource Theme_Background}"
                         Foreground="{DynamicResource Theme_Foreground}"
                         ToolTip="Your API key"/>
                
                <Button x:Name="toggleApiKeyButton"
                        Grid.Column="1"
                        Content="👁"
                        Width="30"
                        Margin="4,0,0,0"
                        Click="ToggleApiKeyVisibility_Click"
                        ToolTip="Show/Hide API Key"
                        Background="{DynamicResource Theme_Background}"
                        Foreground="{DynamicResource Theme_Foreground}"/>
            </Grid>
        </StackPanel>

        <!-- Options -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Options" 
                       FontWeight="Bold" 
                       Margin="0,0,0,8"
                       Foreground="{DynamicResource Theme_Foreground}"/>
            
            <CheckBox x:Name="autoSendOnEnterCheckBox"
                      Content="Auto send on Enter"
                      IsChecked="True"
                      Margin="0,0,0,6"
                      Foreground="{DynamicResource Theme_Foreground}"
                      ToolTip="When enabled, pressing Enter sends the message. Use Shift+Enter for new line."/>
            
            <CheckBox x:Name="agentModeCheckBox"
                      Content="Agent mode"
                      IsChecked="True"
                      Foreground="{DynamicResource Theme_Foreground}"
                      ToolTip="Enable agent mode for multi-step reasoning on complex queries. Recommended for thorough analysis."/>
        </StackPanel>

        <!-- Info -->
        <Border Grid.Row="4" 
                Background="{DynamicResource Theme_InfoBarBackground}"
                BorderBrush="Gray"
                BorderThickness="1"
                Padding="10"
                Margin="0,0,0,12">
            <TextBlock TextWrapping="Wrap"
                       Foreground="{DynamicResource Theme_Foreground}">
                <Run FontWeight="Bold">Configuration Note:</Run>
                <LineBreak/>
                These settings are saved for the current session only.
                <LineBreak/>
                <LineBreak/>
                To persist configuration across sessions, set environment variables:
                <LineBreak/>
                • LLM_ENDPOINT
                <LineBreak/>
                • LLM_API_KEY
                <LineBreak/>
                • LLM_MODEL
            </TextBlock>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="1" 
                    IsDefault="True" 
                    Content="Save"
                    Click="SaveButton_Click"
                    MinWidth="75" 
                    MinHeight="23" 
                    Margin="0,0,10,0" 
                    Padding="15,2"
                    Background="{DynamicResource Theme_Background}"
                    Foreground="{DynamicResource Theme_Foreground}"/>
            <Button Grid.Column="2" 
                    IsCancel="True" 
                    Content="Cancel"
                    MinWidth="75" 
                    MinHeight="23" 
                    Padding="15,2"
                    Background="{DynamicResource Theme_Background}"
                    Foreground="{DynamicResource Theme_Foreground}"/>
        </Grid>
    </Grid>
</Window>
